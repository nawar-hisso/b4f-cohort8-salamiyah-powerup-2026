# TaskFlow

Welcome. Over the next sessions you will build **TaskFlow**, a small task dashboard,
step by step.

Right now this project is almost empty. That is on purpose — we start from the beginning.

---

## How to run it

1. Open this folder in **VS Code**.
2. Install the **Live Server** extension (once).
3. Right-click `index.html` and choose **Open with Live Server**.

The page opens in your browser. When you save a file, the browser refreshes by itself.

You do not need to install anything else. No Node, no npm, no build step.

---

## The files

```
index.html        the structure of the page
css/style.css     how the page looks
js/app.js         what the page does
```

Keep this structure. Do not rename or move the files.

---

## Your branch

Your trainer told you which branch your group uses:

- **Group 1** uses the branch `group-1`
- **Group 2** uses the branch `group-2`

Make sure you are on your group's branch before you start:

```bash
git branch          # shows which branch you are on
git checkout group-1
git pull
```

Work on your own computer during the exercises. Your trainer will tell you if and when
you should push anything.

---

## Documents

Notes and homework will appear in a `docs/` folder after each session.
There is nothing there yet.

---

## Submitting homework

Bring your project to the next session and be ready to **explain one thing you changed**.
Your trainer will tell you the exact hand-in method for your group.
